interface:
  display_name: "KM CLI Doc & Comment"
  short_description: "Run km-cli auth, doc, search, and comment workflows."
  default_prompt: "Use local km-cli to handle auth, create docs, search content, fetch comments, and add comments with safe structured commands."
